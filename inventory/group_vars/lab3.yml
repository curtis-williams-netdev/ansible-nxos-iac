---
# Variables specific to lab3 devices (sandbox environment)
# lab3 is a child group - devices here also belong to their role group (distro, accessleaf, etc.)
# Variables here override parent group vars when targeting lab3

environment: lab
# You can override things like syslog/NTP to point to lab servers instead of prod
